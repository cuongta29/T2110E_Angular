import { AddTextDirective } from './add-text.directive';

describe('AddTextDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new AddTextDirective();
    expect(directive).toBeTruthy();
  });
});
